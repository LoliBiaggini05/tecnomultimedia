function setup() {
  createCanvas(400, 400);
  // Crea una instancia de la clase Casa
  let miCasa = new Casa(100, 200);
  // Muestra la casa en el lienzo
  miCasa.mostrar();
}

function draw() {
  // No se necesita 
}
